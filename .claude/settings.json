{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'git (commit|push)'; then cd \"$CLAUDE_PROJECT_DIR\" && python3 tests/check_static.py || { echo 'Статические проверки не прошли — чини до коммита (tests/README.md).' >&2; exit 2; }; fi",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
